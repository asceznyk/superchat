services:
  backend:
    build: ./backend
    image: diagene_backend:latest
    container_name: dev_backend
    networks:
      - diagene_net
    volumes:
      - ./backend:/app
    expose:
      - "8000"
    restart: unless-stopped

  nginx:
    image: nginx:latest
    container_name: nginx
    depends_on:
      - backend
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - diagene_net
    restart: unless-stopped

  frontend:
    build: ./frontend
    image: diagene_frontend:latest
    container_name: dev_frontend
    networks:
      - diagene_net
    volumes:
      - ./frontend:/app
      - node_modules:/app/node_modules
    expose:
      - "3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=development

networks:
  diagene_net:
    driver: bridge

volumes:
  node_modules:

